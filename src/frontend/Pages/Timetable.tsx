import React from "react";
import { NavBar } from "../Components";

export default function Timetable() {
    return (
        <React.Fragment>
            <NavBar />
        </React.Fragment>
    );
}
